# Port the API listens on (default 4000)
PORT=4000

# Secret used to sign JWTs — change this in production!
JWT_SECRET=parkmitter-dev-secret

# Force the storage backend. Leave unset to auto-detect:
# uses better-sqlite3 when its native module loads, otherwise the JSON file store.
# DB_BACKEND=json
