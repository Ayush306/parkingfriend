# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# Local Android SDK + emulator (multi-GB, machine-specific — never push)
android-sdk/
cmdline-tools.zip
ExpoGo-*.apk

# Native project folders (managed workflow — EAS regenerates them)
android/
ios/

# Server local state (DB + secrets stay off GitHub)
server/node_modules/
server/data/
server/server.log
server/server-err.log
server/.env
server/cloudflared.exe

# Misc
metro.log
*.log
